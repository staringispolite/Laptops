data entry forms
  product, product/post (product data)
  component, component/post
  component_type, component_type/post
  product_component (drop-downs), product_component/post
server port via config file
deploy plan
fabfile
auto-key usage instead of -i every time
Base mako template

